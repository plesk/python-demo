[project]
name = "python-demo"
version = "0.1.0"
description = "A simple demo project to test Plesk Python support."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]
